<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="160dp"
    android:background="@color/colorPrimary"
    android:layout_height="220dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="10dp">

        <androidx.cardview.widget.CardView
            android:id="@+id/play_list_image_card"
            android:layout_width="140dp"
            android:layout_height="140dp"
            app:cardCornerRadius="20dp"
            android:elevation="0dp">

            <ImageView
                android:id="@+id/play_list_image"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:src="@drawable/image_play_list1"
                android:background="@color/white"
                android:scaleType="fitXY"
                />
        </androidx.cardview.widget.CardView>



        <TextView
            android:id="@+id/play_list_song_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/play_list_image_card"
            android:layout_marginTop="15dp"
            android:ellipsize="end"
            android:text="Sound of Sky"
            android:maxLines="1"
            android:scrollHorizontally="true"
            android:textColor="@color/grey_20"
            android:textSize="14sp"
            android:textStyle="normal" />

        <TextView
            android:id="@+id/play_list_artist_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/play_list_song_name"
            android:layout_marginTop="5dp"
            android:ellipsize="end"
            android:text="Piano Guys"
            android:maxLines="1"
            android:scrollHorizontally="true"
            android:textColor="@color/grey_60"
            android:textSize="12sp"
            android:textStyle="normal" />

    </RelativeLayout>


</RelativeLayout>